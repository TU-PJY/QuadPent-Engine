In order to run it after compilation, the items in the dll folder must be included in the X64/Release(or debug) folder.

When distributing exe files, they must be distributed together with the corresponding dll files.